export interface SauceTheme {
  text: string;
  accent: string;
  border: string;
  highlight: string;
  gradient: string;
  buttonHover: string;
  shadow: string;
  mainGradient: string;
  pulseGradient: string;
}

export type ThemeVariant = 'default' | 'warm' | 'cool' | 'nature' | 'berry' | 'sunset';